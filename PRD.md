# AI 智慧园区 — 产品需求文档

> **文档版本** v1.0 | **创建日期** 2026-06-22 | **状态** 待评审

---

## 1. Executive Summary

### 问题陈述
传统园区（学校/产业园）的信息服务依赖人工窗口和静态网页，学生/教职工查找办事流程、园区公告、社团信息效率极低，且信息分散在多个系统，缺乏统一入口。

### 解决方案
构建一个由大模型驱动的 AI 智慧园区平台，通过微信小程序提供统一入口，整合智能问答、资料检索、社团推荐和自动化报名四大智能能力。

### 成功指标（5 项可量化 KPI）

| 指标 | 目标值 | 测量方式 |
|---|---|---|
| 问答准确率 | 常见园区问题 Top-20 准确率 ≥ 90% | 人工标注测试集 |
| RAG 检索命中率 | Top-3 召回率 ≥ 85% | 标注问答对评测 |
| 推荐点击转化率 | 推荐社团点击率 ≥ 30% | 埋点统计 |
| 报名完成耗时 | 从查看到报名完成 < 60 秒 | 端到端计时 |
| API 响应延迟 | 非 AI 接口 P95 < 200ms，AI 流式首字 < 2s | 压测 |

---

## 2. User Experience & Functionality

### 用户画像

| 画像 | 描述 | 核心诉求 |
|---|---|---|
| **学生** | 园区主体用户，18-25 岁 | 找社团、看公告、问办事流程、报名活动 |
| **教职工** | 管理员/教师，25-55 岁 | 发布公告、管理资料库、查看活动数据 |
| **社团负责人** | 管理社团的学生/老师 | 发布纳新信息、查看报名情况 |

### 用户故事

#### 模块一：园区智能咨询

| ID | 用户故事 | 验收标准 |
|---|---|---|
| US-01 | 作为学生，我想用自然语言问"图书馆几点关门"，以便快速获取园区设施信息 | ① 支持文本输入发送消息 ② 返回流式打字机效果 ③ 答案正确引用园区知识库 |
| US-02 | 作为学生，我想问"校园卡丢了怎么补办"，以便获取完整办事流程 | ① 多轮对话保留上下文 ② 返回结构化步骤+链接 |
| US-03 | 作为用户，我点击预设快捷问题可一键提问 | ① 提供 4-6 个高频问题快捷入口 |
| US-04 | 作为用户，我能查看历史对话记录 | ① 会话列表按时间倒序 ② 可删除/清空历史 |

#### 模块二：园区墙 & 资料库智能检索

| ID | 用户故事 | 验收标准 |
|---|---|---|
| US-05 | 作为学生，我想搜索"奖学金申请"，以便找到所有相关公告和文档 | ① 支持语义搜索（非关键词匹配）② 结果按相关度排序 ③ 显示标题+摘要+时间 |
| US-06 | 作为教职工，我想上传 PDF/Word 文档到资料库 | ① 支持 PDF、DOCX、TXT 格式 ② 上传后自动向量化 ③ 给出处理状态反馈 |
| US-07 | 作为用户，我能按分类（通知、活动、制度）浏览公告 | ① 分类 Tab 切换 ② 支持分页加载 |
| US-08 | 作为用户，我能查看园区墙实时动态 | ① 信息流按时间倒序 ② 支持点赞/收藏 |
| US-09 | 作为教职工，我能删除自己发布的公告 | ① 软删除 ② 同步移除向量索引 |

#### 模块三：社团纳新推荐系统

| ID | 用户故事 | 验收标准 |
|---|---|---|
| US-10 | 作为学生，系统根据我的兴趣标签推荐匹配社团 | ① 首次进入引导选择 3-5 个兴趣标签 ② 推荐列表按匹配度排序 ③ 显示社团介绍+纳新名额 |
| US-11 | 作为学生，我可以在推荐页一键报名社团 | ① 填写姓名+学号+手机号 ② 点击报名 → 写入报名表 ③ 防止重复报名 |
| US-12 | 作为社团负责人，我能发布纳新活动并设置名额限制 | ① 填写社团介绍+纳新要求+名额 ② 报名满额后自动下架 |
| US-13 | 作为学生，我能查看自己的报名记录和状态 | ① 列表显示报名社团+时间+状态（已提交/已通过/未通过） |

#### 模块四：数据同步与向量化

| ID | 用户故事 | 验收标准 |
|---|---|---|
| US-14 | 作为系统，当新增/修改公告时，自动同步向量索引 | ① 通过 RabbitMQ 异步处理 ② 失败重试 3 次 ③ 状态可追踪 |
| US-15 | 作为管理员，我能手动触发全量向量重建 | ① 提供管理端触发按钮 ② 显示重建进度 ③ 重建期间查询不中断 |

### 非目标（v1 不做）

- 不做语音输入/语音播报
- 不做实时聊天/IM
- 不做第三方登录（微信授权登录在 MVP 中仅做模拟）
- 不做图片/视频内容的向量检索（仅文本）
- 不做个性化推荐模型的持续训练/微调（使用静态匹配）

---

## 3. AI System Requirements

### AI 能力矩阵

| 能力 | 技术方案 | 说明 |
|---|---|---|
| 对话生成 | OpenAI 兼容 Chat Completion API | 流式输出、多轮对话、System Prompt |
| 语义检索 | Redis Stack (RediSearch) 向量相似度 | HNSW 索引，Cosine 相似度 |
| 文档解析 | Apache Tika / 手动解析 | PDF、DOCX、TXT → 纯文本 |
| 向量化 (Embedding) | OpenAI 兼容 Embedding API (text-embedding-3-small) | 维度 1536 |
| 文本分块 | 固定大小 + 滑动窗口 | chunk_size=512, overlap=50 |
| Function Calling | OpenAI 兼容 Tools API | 调用园区数据接口（设施查询、办事指南等） |
| 兴趣匹配 | 标签 Jaccard 相似度 + LLM 重排序 | 先过滤，再精排 |

### Prompt 工程策略

```
# 园区咨询助手 System Prompt
你是一个智慧园区的AI助手，名叫"园宝"。你的职责：
1. 回答园区相关问题（设施、流程、活动）
2. 你有权调用工具查询实时信息
3. 回答简洁、友善，不超过 200 字
4. 不确定时主动告知，并建议用户去综合服务大厅
5. 如果用户问的是社团/公告，主动引导到对应功能

# Function Call Tool 定义
- query_facility_location(name) → 设施位置和开放时间
- query_procedure(name) → 办事流程和所需材料
- query_announcements(keyword) → 搜索公告和文档
```

### 评测策略

| 评测维度 | 方法 | 门槛 |
|---|---|---|
| 问答准确性 | 20 道人工标注问答对，检查关键信息是否包含 | ≥ 90% |
| RAG 检索质量 | 30 条查询，检查 Top-3 中是否含正确答案 | ≥ 85% 命中 |
| 推荐效果 | 人工评估 20 个用户的推荐结果是否合理 | ≥ 80% 满意度 |
| 幻觉检测 | 检查 20 条回答是否包含虚假信息 | 0 幻觉 |

---

## 4. Technical Specifications

### 架构总览

```
┌──────────────────────────────────────────────────────────┐
│                    微信小程序 (WXML + JS)                   │
│  智能咨询 │ 资料检索 │ 社团推荐 │ 我的报名 │ 园区墙          │
└──────────────────────┬───────────────────────────────────┘
                       │ HTTPS / SSE (流式)
┌──────────────────────▼───────────────────────────────────┐
│                  SpringBoot API Gateway                    │
│  /api/chat │ /api/search │ /api/club │ /api/apply          │
└───┬─────────┬──────────┬──────────┬──────────────────────┘
    │         │          │          │
    ▼         ▼          ▼          ▼
┌──────┐ ┌──────┐ ┌──────┐ ┌──────────┐
│Chat  │ │Search│ │Club  │ │Apply     │
│Svc   │ │Svc   │ │Svc   │ │Svc       │
│      │ │      │ │      │ │          │
│调用AI│ │向量检│ │标签匹│ │报名表    │
│API   │ │索+AI │ │配+LLM│ │CRUD      │
│      │ │重排序│ │重排序│ │          │
└──┬───┘ └──┬───┘ └──┬───┘ └────┬─────┘
   │        │        │           │
   ▼        ▼        ▼           ▼
┌──────────────────────────────────────┐
│            SpringAI 统一适配层         │
│  ChatClient │ EmbeddingClient │ Tools │
└──────┬───────────────────────────────┘
       │ OpenAI 兼容协议
       ▼
┌──────────────┐
│  大模型 API   │
│ (OpenAI /     │
│  兼容服务)    │
└──────────────┘

┌──────────────────────────────────────────────┐
│                  数据层                         │
│  MySQL (业务数据)  Redis Stack (向量+缓存+锁)    │
│  RabbitMQ (异步消息)   MinIO (文件存储)          │
└──────────────────────────────────────────────┘
```

### 技术栈明细

| 层次 | 技术 | 版本要求 | 用途 |
|---|---|---|---|
| 框架 | SpringBoot | 3.x | 基础框架 |
| AI 集成 | SpringAI | 1.0.x | Chat、Embedding、Tool 统一抽象 |
| ORM | Mybatis-Plus | 3.5.x | 数据库访问 |
| 数据库 | MySQL | 8.0+ | 业务数据存储 |
| 缓存/向量 | Redis Stack | 7.2+ | 缓存 + RediSearch 向量检索 |
| 消息队列 | RabbitMQ | 3.12+ | 异步向量化、事件驱动 |
| 对象存储 | MinIO | 最新稳定版 | 文档文件存储 |
| 文档解析 | Apache Tika | 2.x | PDF/DOCX 文本提取 |
| 前端 | 微信小程序原生 | — | 用户端 |
| 构建 | Maven | 3.8+ | Java 构建 |

### 数据库核心表

```sql
-- 用户表 (模拟微信登录)
t_user (id, openid, nickname, avatar_url, interest_tags, ...)

-- 公告/文档表
t_announcement (id, title, content, summary, category, file_url, vector_id, status, ...)

-- 社团表
t_club (id, name, description, tags, logo_url, member_count, ...)

-- 纳新活动表
t_recruitment (id, club_id, title, requirement, quota, enrolled_count, start_time, end_time, status)

-- 报名记录表
t_application (id, recruitment_id, user_id, name, student_id, phone, status, ...)

-- 对话会话表
t_conversation (id, user_id, title, ...)

-- 对话消息表
t_message (id, conversation_id, role, content, ...)
```

### 接口设计（核心 API）

| 方法 | 路径 | 说明 | 流式 |
|---|---|---|---|
| POST | `/api/chat/send` | 发送对话消息 | ✅ SSE |
| GET | `/api/chat/conversations` | 获取会话列表 | ❌ |
| GET | `/api/chat/messages?convId=` | 获取消息历史 | ❌ |
| POST | `/api/search` | 语义搜索公告和文档 | ❌ |
| GET | `/api/announcements?category=&page=&size=` | 分页查询公告 | ❌ |
| POST | `/api/announcements/upload` | 上传文档（教职工） | ❌ |
| GET | `/api/club/recommend?tags=` | 兴趣推荐社团 | ❌ |
| POST | `/api/apply` | 提交报名 | ❌ |
| GET | `/api/apply/my` | 我的报名记录 | ❌ |
| POST | `/api/admin/rebuild-index` | 重建向量索引 | ❌ |

### 安全与隐私

- **认证**：微信小程序 code2Session 换取 openid，服务端生成 JWT
- **授权**：教职工操作（发布公告、管理）需角色校验
- **数据脱敏**：手机号在接口返回时中间 4 位脱敏
- **API 安全**：参数校验、SQL 注入防护（Mybatis-Plus 参数化）、XSS 过滤
- **AI 安全**：Prompt Injection 基础防御（System Prompt 中约束角色边界）

---

## 5. Risks & Roadmap

### 分期路线图

#### 🏷 Phase 0 — 项目脚手架（Day 1）
- SpringBoot 项目搭建 + 多模块
- SpringAI 集成 + 对接大模型 API
- MySQL + Redis + RabbitMQ + MinIO 联通
- 微信小程序项目初始化 + 基础页面骨架
- **产出**：Hello World 级对话可用

#### 🏷 Phase 1 — 核心链路 MVP（Day 2-4）

| 模块 | 范围 |
|---|---|
| 智能咨询 | 单轮 QA（无 RAG）+ 流式输出 + 历史记录 |
| 资料检索 | 文档上传 → 分块 → Embedding → 向量存储 → 搜索 API |
| 社团推荐 | 社团 CRUD + 标签匹配推荐 + 报名提交 |
| 园区墙 | 公告列表 + 分类筛选 + 详情页 |

#### 🏷 Phase 2 — 增强能力（Day 5-6）
- 多轮对话上下文
- Function Calling（设施查询 Tool）
- RAG 问答（检索增强生成）
- 推荐用 LLM 重排序
- 异步向量同步管道（RabbitMQ）

#### 🏷 Phase 3 — 收尾打磨（Day 7）
- 全链路联调
- 边界条件处理
- 教学文档/实验指导书
- 性能压测

### 技术风险

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 大模型 API 不可用/限流 | 核心对话功能不可用 | ① 本地 mock 模式降级 ② 客户端提示"AI 繁忙中" |
| Redis Stack 向量搜索性能不足 | 检索变慢 | HNSW 索引默认适合 10 万级文档，MVP 量级足够 |
| 微信小程序审核不通过 | 无法上线 | MVP 阶段以体验版运行，不提交审核 |
| Embedding 费用过高 | 教学成本 | ① 首次向量化后增量更新 ② 限制文档总量 |
| RabbitMQ 消息积压 | 向量化延迟 | 设置消息 TTL + 死信队列 + 监控告警 |

---

## 附录 A：项目结构（建议）

```
ai-smart-campus/
├── campus-common/          # 公共模块：DTO、异常、枚举、工具类
├── campus-core/            # 核心业务模块
│   ├── chat/               # 智能咨询（ChatClient + SSE）
│   ├── search/             # 资料检索（Embedding + RedisSearch）
│   ├── club/               # 社团推荐 + 报名
│   ├── announcement/       # 园区墙/公告管理
│   ├── admin/              # 管理后台接口（重建索引等）
│   └── common/             # 跨域、拦截器、全局配置
├── campus-infra/           # 基础设施层
│   ├── ai/                 # SpringAI Bean 配置
│   ├── mq/                 # RabbitMQ 生产者/消费者
│   ├── vector/             # Redis 向量操作封装
│   └── storage/            # MinIO 文件操作封装
├── campus-admin/           # 后台管理模块（可选 Spring Boot Admin）
└── sql/                    # 数据库初始化脚本
```

## 附录 B：关键 Skill 触发词速查

| 场景 | 触发方式 |
|---|---|
| 写开发计划 | "根据 PRD 写实施计划" |
| TDD 实现 | `/tdd` 或 "用 TDD 实现 XXX" |
| 审查代码 | `/code-review` |
| 启动验证 | "跑起来看看" 或 `/verify` |
| 安全审查 | "做安全审查" |
